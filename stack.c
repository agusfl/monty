#include "monty.h"
/**
 * stack - adds node to the start of a doubly linked list.
 * @head: head of a doubly linked list.
 * @line_number: bytecode line number
 */

void stack(stack_t **head, unsigned int line_number)
{
	(void)head;
	(void)line_number;
	stack_or_queue = 0;
}

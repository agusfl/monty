#include "monty.h"
/**
 * queue - adds node to the end of a doubly linked list.
 * @head: head of a doubly linked list.
 * @line_number: bytecode line number
 */

void queue(stack_t __attribute__((unused))  **head, unsigned int line_number)
{
	(void)line_number;
	stack_or_queue = 1; /* 1 es queue */
}
